name: Deploy to Hostinger

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ftp://72.60.93.208
          username: u375308597.rosterplus.io
          password: ${{ secrets.FTP_PASSWORD }}
          server-dir: /
          local-dir: ./
          dangerous-clean-slate: false
          exclude: |
            **/.git*
            **/.git*/**
            **/.github/**
            **/node_modules/**
            **/.DS_Store
            **/.claude/**
            **/e2e/**
            **/supabase/**
            **/.mcp.json
            **/netlify.toml
            **/serve.js
            **/supabase-schema.sql
            **/README.md
